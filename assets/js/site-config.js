/*
 * Site configuration
 * ------------------
 * Central place for firm contact details used across the site.
 *
 * Values wrapped in double curly braces are UNRESOLVED PLACEHOLDERS.
 * Replace them with the real values before the site goes live.
 * Until they are replaced, site.js hides or disables the related
 * links and shows a neutral "to be published" label instead.
 */

window.siteConfig = {
  firmName: "H.R. Legal Associate",
  domain: "https://lexlawandtax.com",
  phone: "{{PHONE_NUMBER}}",
  whatsapp: "{{WHATSAPP_NUMBER}}",
  email: "{{EMAIL_ADDRESS}}",
  address: "{{OFFICE_ADDRESS}}",
  googleMapsUrl: "{{GOOGLE_MAPS_URL}}",
  formEndpoint: "{{FORM_ENDPOINT}}"
};
